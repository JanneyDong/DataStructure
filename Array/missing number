
class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        y = []
        for i in range(0,len(nums)+1):
            y.append(i)
        return sum(y) - sum(nums)
